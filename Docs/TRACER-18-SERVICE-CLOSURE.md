# TRACER 18-Service Closure — Overnight Service/Data/Bridge Pass 2026-09-16

Authority: `origin/integration/windows-visual-authority-20260916` @ `1292098a6af8d6d031c8add2e3c6043eae9b8647` (34 ahead of `origin/main` @ `755d514`)
Bridge fix: non-blocking PowerShell preview runner (runPsAsync) + async getSystemSnapshot + async previews. Gate restarts 2 → 0.
Matrix: 18/18 PASS, 158/158 tools, 6/6 families, controlled restarts = 0 (was 2 then budget exhausted).

## Per-service closure matrix

| # | Service | Tools | Data source (Windows native) | Preview endpoint (bridge) | Execution path (manifest ToolIds) | Structured result | Cancel | Verification | Admin | Tests (frontend) | Status | Remaining gap |
|---|---------|-------|-------------------------------|---------------------------|-----------------------------------|-------------------|--------|--------------|-------|------------------|--------|---------------|
| 01 | System Maintenance | 10 | Win32_OperatingSystem, Win32_ComputerSystem, Win32_Processor, Win32_LogicalDisk, Get-Service WinDefend, Get-MpComputerStatus, Win32_LogicalDisk; SM01-10 scripts | `GET /api/system` (SYSTEM_PS, 30s cache) + `POST /api/runs` | SM01-10 (SM01 VerifySystemFiles … SM10 Report) | `Stop-KnouxSession` → `results.json` (`Status`, `ExitCode`, `VerificationResult`) | backend-confirmed via `POST /api/runs/:id/cancel` + `pollUntilTerminal` | SFC `/verifyonly`, DISM `/ScanHealth`/`/RestoreHealth` post-verify; wsr | mixed (SM01-09 admin, SM10 non-admin) | `station01.test.mjs` + `station01Care.test.mjs` (care workflow, DISM chain) | PASS | none — health categorical, inconclusive distinct |
| 02 | System Cleanup | 11 | Win32_LogicalDisk, Get-ChildItem temp paths, `HKCU` recycle bin, browser cache paths; SC01-11 scripts | `GET /api/cleanup/preview` (SC11, `---KNOUX_CLEANUP_JSON_START---`) | SC01-11 (SC01 CleanUserTemp … SC11 InteractiveCleanupPreview) | `results.json` with `bytesMoved`, `verificationResult`; quarantine by default | yes (cooperative, `cancelRun`→`taskkill /T`) | before/after bytes not estimated; quarantined entries verifiable | admin false for user temp, mixed | `station02.test.mjs` | PASS | none — recovered bytes from evidence, not estimate |
| 03 | Network / Internet | 11 | Get-NetAdapter, Get-NetIPConfiguration, Get-NetTCPConnection, Resolve-DnsName, Test-NetConnection; NI01-11 scripts | `GET /api/network/preview` (NI11) | NI01-11 (NI01 … NI11 InteractiveNetworkPreview) | `results.json` + `NetworkPreview` JSON (adapters, safety) | yes | post-action retest (gateway/DNS/internet layers); command exit ≠ restored | NI06-11 admin true for stack reset | `station03.test.mjs` | PASS | none — ICMP-blocked stays inconclusive, not failed |
| 04 | Programs / Applications | 10 | Registry Uninstall keys (HKLM x64, HKLM WOW6432Node, HKCU), Get-AppxPackage (never Win32_Product); PA01-10 scripts | `GET /api/software/preview` (SW07) + PA model parsing (registry) | PA01-10 (PA01 ListInstalledPrograms … PA10) | `results.json` + `SoftwarePreview` inventory; uninstall verification via before/after registry | yes | after inventory no longer registered; protected-path guard | mixed (PA02 etc. admin) | `station04.test.mjs` (44 acceptances) | PASS | none — dedup + classification truthful |
| 05 | Duplicate Files | 11 | Filesystem SHA-256 (Node engine), SQLite index `Data/duplicates-index.db`, Quarantine `Quarantine/DF*`; DF01-11 | `GET /api/duplicates/preview` (DF11) + `POST /api/duplicates/engine-scan` + indexed jobs `/api/duplicates/jobs` | DF01-11 (DF02 quarantine, DF11 preview etc.) | `results.json` + `DuplicatePreview` + job `SCAN/CANCELLED/COMPLETED` | cooperative indexed cancel (`requestCancel`) + keeper/quarantine | restore collision-safe rename + verify SHA-256 | non-admin (reads) except quarantine requires lock | `station05.test.mjs` + `duplicatesEngine.test.mjs` + `duplicatesIndex.test.mjs` | PASS | none — engine + SQLite + quarantine preserved |
| 06 | Disk Space / Storage | 10 | Get-CimInstance Win32_LogicalDisk, Get-PhysicalDisk, Get-Disk, Get-Volume, Get-StorageReliabilityCounter (SMART where exposed); DS01-10 | `GET /api/system` (drives) + DS preview via system snapshot; disk health via DS scripts | DS01-10 (DS01 volume inventory …) | `results.json` + volume capacity evidence | yes | health/inconclusive honest; no fabricated temp/SMART | mixed | `station06.test.mjs` | PASS | none — unknown when hardware does not expose metric |
| 07 | Services / Processes | 11 | Get-Service, Get-CimInstance Win32_Service, Get-Process, startup entries; SP01-11 scripts | `GET /api/operations/preview` (SP11) | SP01-11 (SP01 ListServices … SP11 InteractiveOperationsPreview) | `results.json` + `OperationsPreview` (services/processes topology) | yes | safe stop/start/restart verification via post-query | admin for service control | `station07.test.mjs` | PASS | none — critical services protected via allowlist |
| 08 | Performance | 12 | Get-CimInstance Win32_Processor, Win32_OperatingSystem (pages/sec), Get-Process, powercfg, visual effects registry; PF01-12 scripts | `GET /api/performance/preview` (PF11) + `GET /api/performance/optimization-preview` (PF12) | PF01-12 (PF01 BootDuration … PF12 InteractiveOptimizationPreview) | `results.json` + `PerformancePreview`/`OptimizationPreview` signals | yes | CPU/memory/disk pressure measured; no fake score | non-admin reads, admin for plan change | `station08.test.mjs` | PASS | none — optimization score derived only from measured |
| 09 | Security | 10 | Get-MpComputerStatus, Get-NetFirewallProfile, Get-Service WinDefend, audit policies; SE01-10 scripts | `GET /api/system` (DefenderRunning/Realtime, firewall) + SE scripts | SE01-10 (SE01 …) | `results.json` + SecurityPreview (posture) | yes | Defender false remains `NOT RUNNING`/`DISABLED`, not unknown | admin for scan/remediate | `station09.test.mjs` | PASS | none — null/unknown vs observed false distinct |
| 10 | Diagnostics / Reports | 11 | Get-WinEvent, Get-CimInstance Win32_PnPEntity (problem devices), Win32_DiskDrive SMART, reliability; DR01-11 scripts | `GET /api/diagnostics/preview` (DR11) | DR01-11 (DR01 … DR11 InteractiveDiagnosticsPreview) | `results.json` + `DiagnosticsPreview` (events, reliability, devices, storage) | yes | source/timestamp/severity preserved | non-admin reads | `station10.test.mjs` | PASS | none — no raw PS noise exposed |
| 11 | Backup / Recovery | 5 | Get-ComputerRestorePoint, vssadmin shadow copies, `Data/Backups`, BR01-05 scripts | `GET /api/backup-recovery/preview` (BR04) | BR01-05 (BR01 …) | `results.json` + `BackupRecoveryPreview` (restorePoints, shadowCopies, localBackups) | yes | restore-point count vs capability; file recovery marked unavailable if engine absent | admin for restore | `station11.test.mjs` | PASS | none — deleted-file recovery honestly unavailable |
| 12 | Developer Tools | 13 | Get-Command, where.exe, git --version, node --version, docker version, Get-NetTCPConnection (ports); DT01-13 scripts | `GET /api/software/preview` (SW07) + `GET /api/software-advanced/preview` (SW08) + DT reports via runs | DT01-13 (DT01 Audit … DT13 DependencyLockfile) | `results.json` + `AdvancedSoftwarePreview` + DT `results.json` | yes | 409 serialization; run lifecycle honest | mixed (DT02 admin for cache quarantine) | `station12.test.mjs` + `dtAsyncLifecycle.test.mjs` + `backgroundAnalyzeSerialization.test.mjs` | PASS | none — StrictMode-safe serialization (analyze+analyzeOnly) |
| 13 | Privacy | 4 | HKCU AdvertisingInfo, Privacy TailoredExperiences, Clipboard history, ConsentStore location/mic/camera, Get-DnsClientCache; PR01-04 scripts | `GET /api/privacy/preview` (PR04) | PR01-04 (PR01 Audit … PR04 Preview) | `results.json` + `PrivacyPreview` settings + ActivityEvidence | yes | registry value state vs unknown; RunMRU aggregated counts only | non-admin reads, admin for flush | `station13.test.mjs` | PASS | none — no generic “protected” claim without evidence |
| 14 | Driver Management | 4 | Get-CimInstance Win32_PnPSignedDriver, Get-PnpDevice; DV01-04 scripts | `GET /api/drivers/preview` (DV04) | DV01-04 (DV01 Inventory … DV04 Preview) | `results.json` + `DriversPreview` (signed, provider group, age) | yes | before/after driver version; pnputil result vs installed state | admin for install/rollback | `station14.test.mjs` | PASS | none — verify installed state, not just command exit |
| 15 | System Monitoring | 4 | Get-Service, Get-Process, CPU/memory via CIM, bridge availability; MO01-04 scripts | `GET /api/operations/preview` (SP11 shared) + `GET /api/system` | MO01-04 (MO01 ResourceSnapshot …) | `results.json` + `OperationsPreview` + system snapshot | yes | 15s authoritative probe contract preserved | non-admin | `station15.test.mjs` | PASS | none — timeout not masked with infinite retries |
| 16 | Software Environment | 8 | Registry Uninstall + Get-AppxPackage + Get-Command runtimes + Chrome extension manifests + winget; SW01-08 scripts | `GET /api/software/preview` (SW07) + `GET /api/software-advanced/preview` (SW08) | SW01-08 (SW01 Discover … SW08 AdvancedPreview) | `results.json` + `SoftwarePreview` + `AdvancedSoftwarePreview` | yes | installed vs available vs recommended distinct | non-admin | `station16.test.mjs` | PASS | none — no fabricated catalog |
| 17 | PostInstall Setup | 6 | Win32_OperatingSystem, winget source, Get-WindowsUpdate-like services, drivers offers; PI01-06 scripts | `GET /api/post-install/preview` (PI06) | PI01-06 (PI01 … PI06 Preview) | `results.json` + `PostInstallPreview` (Winget, catalog, pending restart) | yes | checklist not auto-complete on button press | admin for install | `station17.test.mjs` | PASS | none — pending restart signals measured |
| 18 | Project Sonar | 7 | Filesystem scan + languageMap deduplication + Git repo detection (SN07); SN01-07 scripts | `GET /api/sonar/preview?path=` (SN07) + `POST /api/sonar/export` + `POST /api/sonar/analysis` (optional AI) | SN01-07 (SN07 Engine core) | `results.json` + `ProjectSonarPreview` (Workspace, Findings, ServicePlan) | n/a (read-only) | severity-ranked findings + workspace path validation | non-admin reads | `station18.test.mjs` + ProjectSonar.Engine languageMap test | PASS | none — no visual redesign; backend contracts closed |

**Execution honesty preserved**: terminal states `success | error | cancelled | inconclusive`; `running` never converted to success; `pollUntilTerminal` + `rememberRun` + `requestConfirmedCancel` (backend-confirmed) applied. Single-run bridge `activeRunId` + background-analyze serialization (`mode===analyze && analyzeOnly`) intact.

**Mutation security preserved**: origin allowlist (127.0.0.1/localhost:3000/5173), JSON content-type gate, capability token `X-Knoux-Bridge-Token`, manifest allowlist, risk matrix `WINRE_ONLY` blocked, admin check, WinRE block.

**Real-data rule enforced**: every preview returns structured evidence from Windows/CIM/registry; unknown stays `null/unknown/unavailable`; observed `false` stays `false`.

**Visual freeze**: no screen redesign; only async preview runner fix required for data binding.

Overall: 18/18 PASS, 0 remaining P0 blockers.
