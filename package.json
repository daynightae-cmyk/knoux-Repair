{
  "name": "root",
  "private": true,
  "workspaces": [
    "web-frontend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=web-frontend",
    "build": "npm run build --workspace=web-frontend",
    "lint": "npm run typecheck --workspace=web-frontend"
  },
  "dependencies": {
    "express": "^5.2.1"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "esbuild": "^0.28.2",
    "tsx": "^4.23.13"
  }
}
